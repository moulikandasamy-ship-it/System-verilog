class transaction;

   rand bit a;
   rand bit b;
   rand bit cin;

   bit sum;
   bit carry;

   function void display(string name);
      $display("[%s] a=%0d b=%0d cin=%0d sum=%0d carry=%0d",
                name,a,b,cin,sum,carry);
   endfunction

endclass
